import Head from 'next/head'
import Layout from '../components/Layout'
import Image from 'next/image'
import { Inter } from '@next/font/google'
import styles from '../styles/Home.module.css'
import client from '../sanity'
import { useEffect,useState } from 'react'
import Link from 'next/link'


const inter = Inter({ subsets: ['latin'] })

export default function Home() {
  console.log("Hello world");

  const [posts, getPosts] = useState([]);
  useEffect(()=>{
    const query=`*[_type=="post"]{...}`;
    client.fetch(query).then(data=>{
      getPosts(data);
    })
  },[]);
  console.log(posts);
  return (
    <>
      
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      
      {posts?.map(post =>{
        return(
        <Link key={post._id} href="/post/${post.slug.current}">
          <div className="overflow-hidden border rounded-lg cursor-pointer group">
            {/* <img className="object-cover w-full transition-transform duration-200 ease-in-out h-60 group-hover:scale-105" src={{urlFor(post.image).url()}} alt="" /> */}
            <div className="flex justify-between p-5 bg-white">
              <div>
                <p className="text-lg font-bold">{post.title}</p>
                <p className="text-xs">{post.description} by {post.author.name}</p>
              </div>
              {/* <img className="w-12 h-12 rounded-full" src={{urlFor(post.author.image).url()}} alt="" /> */}
            </div>
          </div>
        </Link>
        )
      }
       
        
        
      )}
    </>
  )
}
